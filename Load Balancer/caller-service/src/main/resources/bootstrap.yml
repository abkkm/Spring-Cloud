spring:
  application:
    name: caller-service
  cloud:
    consul:
      host: localhost
      discovery:
        register-health-check: true    
        healthCheckInterval: 5s
        hostname: 10.0.75.1        