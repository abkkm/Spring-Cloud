spring:
  application:
    name: caller-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        register-health-check: true    
        healthCheckInterval: 5s
        hostname: 10.0.75.1        